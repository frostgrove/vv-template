services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_USER: app
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
